# Python dependencies for building the documentation site (site/).
# corehydropy itself is installed separately from the repo (pip install ./corehydropy).
quartodoc>=0.11
griffe<2  # quartodoc 0.11 is incompatible with griffe 2.x parser options
jupyter
matplotlib>=3.7
numpy>=1.24
pandas>=2.0
