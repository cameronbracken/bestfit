% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distribution.R
\name{dist_properties}
\alias{dist_properties}
\alias{dist_moments}
\alias{dist_params}
\alias{dist_lmoments}
\alias{dist_log_likelihood}
\title{Distribution properties}
\usage{
dist_moments(d)

dist_params(d)

dist_lmoments(d)

dist_log_likelihood(d, data)
}
\arguments{
\item{d}{a \code{corehydro_dist} object from \code{\link[=distribution]{distribution()}} or \code{\link[=dist_fit]{dist_fit()}}.}

\item{data}{numeric vector of observations.}
}
\value{
\code{dist_moments()} returns a named numeric vector (\code{mean}, \code{median},
\code{mode}, \code{sd}, \code{skewness}, \code{kurtosis}, \code{minimum}, \code{maximum}; entries are
\code{NaN} where undefined). \code{dist_params()} returns the parameter vector named
with the family's short-form parameter names. \code{dist_lmoments()} returns
the first four L-moments (errors for families without L-moment support).
\code{dist_log_likelihood()} returns a single numeric value.
}
\description{
Moments, parameters, linear moments (L-moments), and log-likelihood of a
\code{\link[=distribution]{distribution()}} object.
}
\examples{
d <- distribution("Normal", c(100, 15))
dist_moments(d)
dist_params(d)
}
