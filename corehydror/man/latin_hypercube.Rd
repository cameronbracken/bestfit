% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{latin_hypercube}
\alias{latin_hypercube}
\title{Latin hypercube sampling}
\usage{
latin_hypercube(n, dimension = 1, seed = NULL, median = FALSE)
}
\arguments{
\item{n}{number of samples (rows).}

\item{dimension}{number of dimensions (columns).}

\item{seed}{integer seed for reproducible samples; \code{NULL} (the default)
seeds from the clock.}

\item{median}{if \code{TRUE}, place each sample at the center of its bin
(median LHS) instead of at a random position within it.}
}
\value{
An \code{n} by \code{dimension} numeric matrix of probabilities in
\verb{(0, 1)}.
}
\description{
Stratified uniform \verb{[0, 1]} samples via Latin hypercube sampling, using
the same seeded Mersenne Twister stream as the C# \code{LatinHypercube.Random},
so a given \code{seed} reproduces the C# sample bit-for-bit (and matches
\code{corehydropy} exactly).
}
\examples{
latin_hypercube(5, 2, seed = 12345)
}
