% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{estimation_diagnostics}
\alias{estimation_diagnostics}
\title{Bayesian estimation diagnostics (leverage / influence / prior influence)}
\usage{
estimation_diagnostics(
  data,
  distribution,
  sampler = "DEMCz",
  iterations = 3000L,
  output_length = 10000L,
  seed = 12345L,
  thinning_interval = -1L,
  thin_every = 10L
)
}
\arguments{
\item{data}{numeric vector of observations.}

\item{distribution}{distribution family name (any name the core distribution factory accepts).}

\item{sampler}{MCMC sampler: \code{"DEMCz"} (default), \code{"DEMCzs"}, \code{"ARWMH"}, or \code{"NUTS"}.}

\item{iterations}{number of post-warmup MCMC iterations.}

\item{output_length}{number of posterior samples retained.}

\item{seed}{PRNG seed for the sampler.}

\item{thinning_interval}{MCMC thinning interval; \code{-1} (default) keeps the sampler's own default.}

\item{thin_every}{prior-influence posterior thinning stride (default \code{10}).}
}
\value{
A named list with three sub-lists: \code{leverage} (\code{index}, \code{leverage}, \code{fit_influence},
\code{variance_influence}, \code{value} arrays; \code{prior_leverage}; \code{total_leverage},
\code{total_fit_influence}, \code{total_variance_influence}), \code{influence} (\code{pareto_k}, \code{elpd_loo} arrays;
\code{count}, \code{mean_pareto_k}, \code{max_pareto_k}, \code{count_pareto_k_above_05}/\verb{_07}/\verb{_10},
\code{proportion_problematic}, \code{is_reliable}), and \code{prior_influence} (\code{count},
\code{prior_precision_share}, \code{total_prior_log_likelihood}, \code{total_data_log_likelihood},
\code{prior_to_data_ratio}, \code{is_prior_influential}, \code{mean_prior_precision_share}).
}
\description{
Fit a univariate distribution to a sample with a Bayesian MCMC analysis and compute the three
estimation diagnostics: observation leverage (from the posterior Hessian at the MAP), PSIS-LOO
influence (per-observation Pareto-k), and prior influence (prior-vs-data log-likelihood share).
Wraps the shared C++ \code{BayesianAnalysis} diagnostics.
}
\examples{
peaks <- c(12500, 15300, 8900, 22100, 18700, 14200, 9800, 28500, 17400, 11600)
d <- estimation_diagnostics(peaks, "Normal", iterations = 100, output_length = 200,
                            seed = 12345, thinning_interval = 1)
d$influence$max_pareto_k
}
