% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{rating_curve_analysis}
\alias{rating_curve_analysis}
\title{Stage-discharge rating-curve frequency analysis}
\usage{
rating_curve_analysis(
  stage,
  discharge,
  segments = 1L,
  stage_bins = NULL,
  min_stage = NULL,
  max_stage = NULL,
  sampler = "DEMCz",
  iterations = 3000L,
  output_length = 10000L,
  credible_level = 0.9,
  seed = 12345L,
  number_of_chains = 4L,
  thinning_interval = -1L
)
}
\arguments{
\item{stage, discharge}{numeric vectors of date-aligned stage / discharge observations.}

\item{segments}{number of rating-curve segments (default \code{1}).}

\item{stage_bins}{number of stage grid points (default \code{NULL}, keeps the data-derived default).}

\item{min_stage, max_stage}{optional stage-grid bounds (default \code{NULL}, data-derived).}

\item{sampler}{MCMC sampler: \code{"DEMCz"} (default), \code{"DEMCzs"}, \code{"ARWMH"}, or \code{"NUTS"}.}

\item{iterations}{number of post-warmup MCMC iterations.}

\item{output_length}{number of posterior samples used to build the credible band.}

\item{credible_level}{credible-interval width (e.g. \code{0.90} for a 90\% band).}

\item{seed}{PRNG seed for the sampler (fixed for reproducibility).}

\item{number_of_chains}{number of MCMC chains (default \code{4}).}

\item{thinning_interval}{MCMC thinning interval; \code{-1} (default) keeps the sampler's own default.}
}
\value{
A named list: \code{parameters}, \code{mode_curve}, \code{mean_curve}, \code{lower_ci}, \code{upper_ci}, \code{aic},
\code{bic}, \code{dic}, \code{rmse}.
}
\description{
Fit a stage-discharge rating curve with a Bayesian MCMC and return the predicted-discharge
mode/mean curve + credible band across a stage grid. Wraps the shared C++ \code{RatingCurveAnalysis}.
}
