% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{ar_analysis}
\alias{ar_analysis}
\title{Bayesian autoregressive AR(p) time-series analysis}
\usage{
ar_analysis(
  data,
  order_p = 1L,
  include_intercept = TRUE,
  training_time_steps = NULL,
  forecasting_time_steps = 0L,
  sampler = "DEMCz",
  iterations = 3000L,
  output_length = 10000L,
  credible_level = 0.9,
  seed = 12345L,
  thinning_interval = -1L
)
}
\arguments{
\item{data}{numeric vector of the observed time series (in sequence order).}

\item{order_p}{autoregressive order (default \code{1}).}

\item{include_intercept}{logical; include an intercept term (default \code{TRUE}).}

\item{training_time_steps}{number of leading steps used for calibration; when \code{NULL} the model
default (\code{max(30, floor(0.8 * n))}) is used, which is invalid for short series -- set it
explicitly (e.g. \code{15}) when \code{n} is small.}

\item{forecasting_time_steps}{number of steps to forecast past the observed series (default \code{0}).}

\item{sampler}{MCMC sampler: \code{"DEMCz"} (default), \code{"DEMCzs"}, \code{"ARWMH"}, or \code{"NUTS"}.}

\item{iterations}{number of post-warmup MCMC iterations.}

\item{output_length}{number of posterior samples used to build the credible band.}

\item{credible_level}{credible-interval width (e.g. \code{0.90} for a 90\% band).}

\item{seed}{PRNG seed for the sampler (fixed for reproducibility).}

\item{thinning_interval}{MCMC thinning interval; \code{-1} (default) keeps the sampler's own default.}
}
\value{
A named list: \code{parameters} (posterior point estimate), \code{mode_curve}, \code{mean_curve},
\code{lower_ci}, \code{upper_ci} (the forecast + credible band over the observed range plus the forecast
horizon), and the scalars \code{aic}, \code{bic}, \code{dic}, \code{rmse}.
}
\description{
Fit an AR(p) autoregressive model with a Bayesian MCMC analysis and return the posterior
forecast curve, credible band, and goodness-of-fit scalars. Wraps the shared C++ \code{ARAnalysis}.
}
\examples{
x <- c(10.2, 11.5, 9.8, 12.1, 13.4, 11.9, 10.6, 12.8, 14.0, 13.1, 11.7, 12.5, 13.9, 15.2,
       14.1, 12.9, 13.6, 15.0, 16.2, 14.8)
fit <- ar_analysis(x, order_p = 1, training_time_steps = 15, forecasting_time_steps = 3,
                   iterations = 100, output_length = 200, seed = 12345, thinning_interval = 1)
length(fit$mode_curve)
}
