% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{plotting_positions}
\alias{plotting_positions}
\title{Plotting positions}
\usage{
plotting_positions(
  n,
  method = c("weibull", "median", "blom", "cunnane", "gringorten", "hazen"),
  alpha = NULL
)
}
\arguments{
\item{n}{the sample size.}

\item{method}{one of \code{"weibull"} (\code{alpha = 0}, the default), \code{"median"}
(\code{0.3175}), \code{"blom"} (\code{0.375}), \code{"cunnane"} (\code{0.4}), \code{"gringorten"}
(\code{0.44}), or \code{"hazen"} (\code{0.5}). Ignored when \code{alpha} is given.}

\item{alpha}{optional explicit plotting-position parameter in \verb{[0, 0.5]}.}
}
\value{
A numeric vector of \code{n} non-exceedance probabilities for the
ordered sample.
}
\description{
Empirical plotting positions \code{(i - alpha) / (n + 1 - 2 * alpha)} for a
sample of size \code{n}, by named method or explicit \code{alpha}.
}
\examples{
plotting_positions(10)
plotting_positions(10, method = "cunnane")
}
