% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{coincident_frequency_analysis}
\alias{coincident_frequency_analysis}
\title{Coincident-frequency analysis}
\usage{
coincident_frequency_analysis(
  marginal_x_family,
  marginal_x_data,
  marginal_x_parameters,
  marginal_y_family,
  marginal_y_data,
  marginal_y_parameters,
  x_values,
  y_values,
  response,
  number_of_bins = 50L,
  copula = "Normal",
  estimation_method = "InferenceFromMargins",
  sampler = "DEMCz",
  iterations = 3000L,
  output_length = 10000L,
  credible_level = 0.9,
  seed = 12345L,
  number_of_chains = 4L,
  thinning_interval = -1L
)
}
\arguments{
\item{marginal_x_family, marginal_y_family}{marginal distribution family names.}

\item{marginal_x_data, marginal_y_data}{numeric vectors of the marginal samples.}

\item{marginal_x_parameters, marginal_y_parameters}{numeric fixed marginal parameter vectors.}

\item{x_values, y_values}{numeric ascending vectors of the primary (X) / secondary (Y) ordinates.}

\item{response}{numeric M x N matrix of the response surface \code{Z[i, j] = f(x_i, y_j)}.}

\item{number_of_bins}{number of Z output bins (default \code{50}).}

\item{copula}{copula family name (e.g. \code{"Normal"}, \code{"Clayton"}, \code{"Gumbel"}).}

\item{estimation_method}{copula estimation method (default \code{"InferenceFromMargins"}).}

\item{sampler}{MCMC sampler: \code{"DEMCz"} (default), \code{"DEMCzs"}, \code{"ARWMH"}, or \code{"NUTS"}.}

\item{iterations}{number of post-warmup MCMC iterations.}

\item{output_length}{number of posterior samples used to build the credible band.}

\item{credible_level}{credible-interval width (e.g. \code{0.90} for a 90\% band).}

\item{seed}{PRNG seed for the sampler (fixed for reproducibility).}

\item{number_of_chains}{number of MCMC chains (default \code{4}).}

\item{thinning_interval}{MCMC thinning interval; \code{-1} (default) keeps the sampler's own default.}
}
\value{
A named list: \code{z_output_values} (the Z bins), \code{mode_curve}, \code{mean_curve}, \code{lower_ci},
\code{upper_ci} (the AEP curve + credible band, one entry per Z bin).
}
\description{
Combine a fitted bivariate (copula) analysis with an M x N response surface \code{Z = f(X, Y)} to
derive the annual-exceedance-probability curve of \code{Z} via the conditional-frequency law of total
probability. Wraps the shared C++ \code{CoincidentFrequencyAnalysis} (which internally fits the
bivariate analysis from the same marginal/copula spec).
}
