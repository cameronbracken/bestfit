% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{posterior_predictive_check}
\alias{posterior_predictive_check}
\title{Posterior predictive check}
\usage{
posterior_predictive_check(
  data,
  distribution,
  sampler = "DEMCz",
  iterations = 3000L,
  output_length = 10000L,
  seed = 12345L,
  number_of_replicates = 1000L,
  thinning_interval = -1L
)
}
\arguments{
\item{data}{numeric vector of observations (also the observed data for the p-value comparison).}

\item{distribution}{distribution family name.}

\item{sampler}{MCMC sampler: \code{"DEMCz"} (default), \code{"DEMCzs"}, \code{"ARWMH"}, or \code{"NUTS"}.}

\item{iterations}{number of post-warmup MCMC iterations.}

\item{output_length}{number of posterior samples used to build the credible band.}

\item{seed}{PRNG seed for the sampler (fixed for reproducibility).}

\item{number_of_replicates}{number of posterior replicate datasets (default \code{1000}).}

\item{thinning_interval}{MCMC thinning interval; \code{-1} (default) keeps the sampler's own default.}
}
\value{
A named list: \code{number_of_replicates}, \code{mean_p_value}, \code{sd_p_value}, \code{skewness_p_value},
\code{min_p_value}, \code{max_p_value}, and \code{has_misfit} (\code{1} when any p-value is extreme, else \code{0}).
}
\description{
Fit a Bayesian MCMC over a model, draw replicate datasets from the posterior, and compute the
common posterior predictive p-values (mean / SD / skewness / min / max) plus a misfit flag.
Wraps the shared C++ \code{PosteriorPredictiveCheck}.
}
