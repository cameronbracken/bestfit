% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{bootstrap_analysis}
\alias{bootstrap_analysis}
\title{Parametric bootstrap confidence bands for a distribution}
\usage{
bootstrap_analysis(
  data,
  distribution,
  probabilities,
  estimation_method = "MaximumLikelihood",
  sample_size = NULL,
  replications = 1000L,
  seed = 12345L,
  alpha = 0.1
)
}
\arguments{
\item{data}{numeric vector of observations.}

\item{distribution}{distribution family name to fit + bootstrap.}

\item{probabilities}{numeric vector of non-exceedance probabilities at which to tabulate the
quantile curve + confidence band.}

\item{estimation_method}{fit method: \code{"MaximumLikelihood"} (default), \code{"MethodOfMoments"}, or
\code{"MethodOfLinearMoments"}.}

\item{sample_size}{bootstrap sample size (default \code{NULL}, uses \code{length(data)}).}

\item{replications}{number of bootstrap replications (default \code{1000}).}

\item{seed}{PRNG seed for the bootstrap.}

\item{alpha}{significance level for the two-sided percentile band (default \code{0.1} -> 90\% band).}
}
\value{
A named list: \code{parameters} (fitted), \code{mode_curve} (fitted-distribution quantiles),
\code{mean_curve}, \code{lower_ci}, \code{upper_ci} (percentile band, one entry per probability).
}
\description{
Fit \code{distribution} to \code{data}, then resample it \code{replications} times to derive percentile
confidence bands on the quantile curve. Wraps the shared C++ \code{BootstrapAnalysis} (Numerics).
}
