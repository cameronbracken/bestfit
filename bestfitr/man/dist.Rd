% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distribution.R
\name{dist}
\alias{dist}
\alias{dist_pdf}
\alias{dist_log_pdf}
\alias{dist_cdf}
\alias{dist_quantile}
\alias{dist_random}
\title{Distribution functions}
\usage{
dist_pdf(d, x)

dist_log_pdf(d, x)

dist_cdf(d, q)

dist_quantile(d, p)

dist_random(d, n, seed = NULL)
}
\arguments{
\item{d}{a \code{bestfit_dist} object from \code{\link[=distribution]{distribution()}} or \code{\link[=dist_fit]{dist_fit()}}.}

\item{x, q}{numeric vector of quantiles.}

\item{p}{numeric vector of probabilities in \verb{(0, 1)}.}

\item{n}{number of draws.}

\item{seed}{integer seed for reproducible draws; \code{NULL} (the default) seeds
from the clock.}
}
\value{
A numeric vector the same length as \code{x}, \code{q}, or \code{p} (\code{n} for
\code{dist_random()}).
}
\description{
Density, log-density, distribution, quantile, and random-generation
functions for a \code{\link[=distribution]{distribution()}} object. All are vectorized over their
first numeric argument and evaluate in the shared C++ core, so results are
identical to the Python package and to the upstream C# library.
}
\details{
\code{dist_random()} draws from the same seeded Mersenne Twister stream as the
C# \code{GenerateRandomValues(sampleSize, seed)}: a given \code{seed} reproduces the
C# draws bit-for-bit (and matches \code{bestfitpy} exactly).
}
\examples{
d <- distribution("Gumbel", c(100, 10))
dist_pdf(d, c(95, 100, 120))
dist_quantile(d, c(0.5, 0.9, 0.99))
dist_random(d, 5, seed = 123)
}
